<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Inicio</title>
    <style>
        /* Estilos de la página */
        body {
            background-color: #f5f7fa;
            font-family: Arial, sans-serif;
            color: #333;
        }
        .container {
            max-width: 1000px;
            margin-top: 50px;
        }
        h1 {
            text-align: center;
            color: #007bff;
            margin-bottom: 40px;
        }
        /* Tarjetas de gestión */
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        .card:hover {
            transform: scale(1.05);
        }
        .card-title {
            font-size: 1.5rem;
            color: #333;
        }
        .card-text {
            color: #666;
        }
        .btn-primary {
            background-color: #007bff;
            border: none;
            font-weight: bold;
        }
        /* Íconos en las tarjetas */
        .icon {
            font-size: 3rem;
            color: #007bff;
            margin-bottom: 15px;
        }
        /* Botón de cerrar sesión */
        .btn-logout {
            display: block;
            width: 100%;
            margin-top: 30px;
            font-size: 1.2rem;
            font-weight: bold;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Bienvenido, <%= user %>!</h1>
        
        <!-- Tarjetas de gestión -->
        <div class="row g-4">
            <div class="col-md-4">
                <div class="card text-center">
                    <div class="card-body">
                        <div class="icon">👤</div>
                        <h5 class="card-title">Gestión de Usuarios</h5>
                        <p class="card-text">Crear, listar, editar y eliminar usuarios en el sistema.</p>
                        <a href="/users" class="btn btn-primary">Ir a Gestión de Usuarios</a>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card text-center">
                    <div class="card-body">
                        <div class="icon">🔐</div>
                        <h5 class="card-title">Gestión de Roles</h5>
                        <p class="card-text">Gestionar roles para los distintos permisos de usuarios.</p>
                        <a href="/roles" class="btn btn-primary">Ir a Gestión de Roles</a>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card text-center">
                    <div class="card-body">
                        <div class="icon">📝</div>
                        <h5 class="card-title">Gestión de Tareas</h5>
                        <p class="card-text">Crear, listar, editar y eliminar tareas asignadas a usuarios.</p>
                        <a href="/tasks" class="btn btn-primary">Ir a Gestión de Tareas</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Botón de Cerrar Sesión -->
        <a href="/logout" class="btn btn-danger btn-logout">Cerrar Sesión</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
